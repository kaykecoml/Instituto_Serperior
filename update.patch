diff --git a/css/pokemon-view.css b/css/pokemon-view.css
index 339734f9c302f94171cb4e2d57c7ee673f3d4d42..e4195de3fb2dfd0d5baaa385e8b3ab89f755d316 100644
--- a/css/pokemon-view.css
+++ b/css/pokemon-view.css
@@ -966,50 +966,58 @@ white-space: pre-line;
 .dex-toggle.active {
   background: #1c573a;
 }
 
 
 @media (max-width: 1024px) {
 
   .pokemon-view {
     grid-template-columns: minmax(0, 1fr);
     grid-template-rows: auto;
     padding: clamp(10px, 3.5vw, 16px);
     gap: clamp(12px, 3vw, 18px);
   }
 
   .left-column,
   .info-panel,
   .stats-panel,
   .move-tabs,
   .evolution-panel {
     grid-column: auto !important;
     grid-row: auto !important;
     max-width: 100%;
     width: 100%;
   }
 
+  .info-panel,
+  .stats-panel,
+  .evolution-panel {
+    width: min(100% - 20px, 640px);
+    margin-left: auto;
+    margin-right: auto;
+  }
+
   .info-panel,
   .stats-panel,
   .move-tabs,
   .evolution-panel,
   .image-panel {
     border-radius: 12px;
     padding: clamp(10px, 3vw, 14px);
   }
 
   button,
   .tab-btn,
   .pixel-button,
   .form-base-btn {
     min-height: 38px;
   }
 
   .dex-search {
     width: min(100% - 20px, 640px);
     flex-wrap: wrap;
     gap: 8px;
   }
 
   .dex-search input,
   .dex-search button {
     width: 100%;
@@ -1087,83 +1095,107 @@ white-space: pre-line;
     flex-wrap: nowrap;
     gap: 6px;
     overflow-x: auto;
     justify-content: flex-start;
     padding-bottom: 8px;
   }
 
   .tab-btn {
     flex: 1 1 auto;
     min-width: clamp(110px, 35vw, 180px);
     text-align: center;
     padding: 8px 10px;
     font-size: clamp(10px, 2.6vw, 12px);
     white-space: nowrap;
   }
 
   .move-tab-buttons .tab-btn {
     min-width: clamp(130px, 44vw, 220px);
   }
 
   .tab-content {
     width: 100%;
     overflow: hidden;
   }
 
+  .info-panel > .block > h3,
+  .info-panel .habitats-panel > h3 {
+    text-align: center;
+  }
+
+  .stats-panel .tab-buttons {
+    justify-content: center;
+  }
+
+  .stats-panel .stats-container,
+  .stats-panel #contest-tab .radar-column {
+    display: flex;
+    flex-direction: column;
+    align-items: center;
+    justify-content: center;
+  }
+
   .stats-tabs canvas,
   .radar-column canvas {
     width: clamp(160px, 48vw, 220px) !important;
     height: clamp(160px, 48vw, 220px) !important;
     max-width: 100%;
   }
 
   .stats-list {
     width: 100%;
     font-size: clamp(10px, 2.8vw, 13px);
   }
 
   .move-table {
     min-width: 620px;
     font-size: 10px;
   }
 
   .move-table th,
   .move-table td {
     white-space: nowrap;
   }
 
   .capacities-wrapper {
     gap: 8px;
     flex-wrap: wrap;
   }
 
   .capacity {
     min-width: 74px;
     flex: 1 1 30%;
   }
 
   #form-buttons {
+    width: min(100%, 360px);
+    margin: 8px auto 0;
+    padding: 6px;
+    border: 2px solid #2f6b44;
+    border-radius: 10px;
+    background: #08120d;
     gap: 4px;
+    -webkit-overflow-scrolling: touch;
+    touch-action: pan-x;
   }
 
   .form-base-btn,
   .form-buttons .tab-btn {
     height: 32px;
     padding: 2px 6px;
     font-size: 9px;
   }
 
   .form-btn {
     width: 28px;
     height: 28px;
   }
 
   .dex-content {
     max-height: min(40vh, 280px);
   }
 
   .nav-zone,
   .nav-btn {
     display: none;
   }
 }
-
