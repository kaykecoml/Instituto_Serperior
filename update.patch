diff --git a/css/moves.css b/css/moves.css
index 3c654f96bb4f3d0dbd613495a66d35e7a53aab34..1553ea2d76af3e11e6ca78ce9fd5cab7676ce863 100644
--- a/css/moves.css
+++ b/css/moves.css
@@ -43,56 +43,82 @@ header {
   left: 20px;
   width: 90px;
   filter: drop-shadow(0 0 6px #7bdc65);
 }
 
 .site-title {
   font-family: "Bully";
   font-size: 52px;
   color: #7bdc65;
  text-shadow:
     0 0 4px rgba(63, 174, 106, 0.6),
     0 0 8px rgba(63, 174, 106, 0.4);
 }
 
 .site-subtitle {
   font-family: "Pokemon";
   font-size: 28px;
   letter-spacing: 3px;
   color: #f8d030;
   text-shadow: 0 0 6px #f8d030;
 }
 
 .site-home-link {
   color: inherit;
   text-decoration: none;
+  display: inline-block;
+  outline: none;
+  -webkit-tap-highlight-color: transparent;
 }
 
 .site-home-link:hover {
   text-decoration: underline;
 }
 
+.site-home-link:focus,
+.site-home-link:focus-visible {
+  outline: none;
+  box-shadow: none;
+}
+
+.site-home-link.expanding {
+  animation: titleExpand 0.36s ease forwards;
+}
+
+@keyframes titleExpand {
+  0% {
+    transform: scale(1);
+    letter-spacing: normal;
+    text-shadow: 0 0 6px rgba(123, 220, 101, 0.4);
+  }
+  100% {
+    transform: scale(1.08);
+    letter-spacing: 1.5px;
+    text-shadow: 0 0 16px rgba(123, 220, 101, 0.9);
+  }
+}
+
 h1 {
   font-family: "Bully";
   font-size: 48px;
   color:  #9ae06f;
 }
 
 h2 {
   font-family: "Pokemon";
   font-size: 32px;
   letter-spacing: 2px;
   margin-top: 5px;
   text-align: center;
 }
 
 .titles {
   text-align: center;
 }
 
 .move-name {
   font-family: "Pixel";
   font-size: 18px;
   letter-spacing: 1px;
   text-shadow: 0 0 6px currentColor;
 }
 
diff --git a/css/pokemon-view.css b/css/pokemon-view.css
index 52596466134d888d1b70956705c5a06cd48e4226..d9bd58a417bff8afc82541c9b6a3fae84261a53f 100644
--- a/css/pokemon-view.css
+++ b/css/pokemon-view.css
@@ -39,56 +39,82 @@ body {
 
 .info-panel .types img {
   width: 36px;
 }
 
 .block {
   margin-top: 14px;
 }
 
 .block h3 {
   font-family: Pixel;
   font-size: 14px;
   color: #7bdc65;
   margin-bottom: 4px;
 }
 
 
 .block #size,
 .block #egg-group {
   font-family: Pixel;
 }
 
 .site-home-link {
   color: inherit;
   text-decoration: none;
+  display: inline-block;
+  outline: none;
+  -webkit-tap-highlight-color: transparent;
 }
 
 .site-home-link:hover {
   text-decoration: underline;
 }
 
+.site-home-link:focus,
+.site-home-link:focus-visible {
+  outline: none;
+  box-shadow: none;
+}
+
+.site-home-link.expanding {
+  animation: titleExpand 0.36s ease forwards;
+}
+
+@keyframes titleExpand {
+  0% {
+    transform: scale(1);
+    letter-spacing: normal;
+    text-shadow: 0 0 6px rgba(123, 220, 101, 0.4);
+  }
+  100% {
+    transform: scale(1.08);
+    letter-spacing: 1.5px;
+    text-shadow: 0 0 16px rgba(123, 220, 101, 0.9);
+  }
+}
+
 .cry-btn {
   margin-top: 16px;
   width: 100%;
   background: #2f6b44;
   border: none;
   padding: 10px;
   border-radius: 10px;
   font-family: Pixel;
   cursor: pointer;
 }
 
 .image-panel {
   text-align: center;
 }
 
 .dex-title {
   font-family: Pixel;
   font-size: 18px;
   margin-bottom: 10px;
 }
 
 
 .art-wrapper {
   display: flex;
   align-items: center;
@@ -626,85 +652,103 @@ body {
   transform: scale(1.1);
 }
 
 /* botão base */
 .form-base-btn {
   background: #08120d;
   border: 1px solid #2f6b44;
   color: #7bdc65;
   font-family: Pixel;
   font-size: 10px;
   padding: 3px 8px;
   border-radius: 6px;
   cursor: pointer;
 }
 
 /* formas regionais */
 .form-buttons .tab-btn {
   padding: 3px 6px;
   font-size: 10px;
   border-radius: 6px;
 }
 
 .evolution-panel {
   grid-column: 2 / 3;
   grid-row: 3;
-  max-width: 320px;
+  width: 100%;
+  max-width: 420px;
   padding: 16px;
   border-radius: 16px;
   margin-top: 24px;
   position: relative;
 }
 
 .evolution-panel::before {
   content: "";
   position: absolute;
   top: 44px;
   bottom: 16px;
   left: 14px;
   width: 3px;
   border-radius: 999px;
   background: rgba(24, 77, 44, 0.9);
 }
 
 .evolution-title {
   font-family: Pixel;
   text-align: center;
   font-size: 14px;
   margin: 0 0 12px;
 }
 
 .evolution-chain.vertical {
   display: flex;
   flex-direction: column;
   align-items: center;
   gap: 12px;
-  flex-wrap: nowrap; /* NÃO quebra linha */
-  max-height: 360px;
+  width: 100%;
+  flex-wrap: nowrap;
+  max-height: 420px;
   overflow-y: auto;
   padding-right: 6px;
   padding-left: 16px;
+  scrollbar-width: thin;
+  scrollbar-color: #63d96f rgba(14, 34, 24, 0.4);
+}
+
+.evolution-chain.vertical::-webkit-scrollbar {
+  width: 6px;
+}
+
+.evolution-chain.vertical::-webkit-scrollbar-track {
+  background: rgba(14, 34, 24, 0.55);
+  border-radius: 999px;
+}
+
+.evolution-chain.vertical::-webkit-scrollbar-thumb {
+  background: linear-gradient(180deg, #7bdc65, #42a850);
+  border-radius: 999px;
 }
 
 .evo-node {
   display: flex;
   flex-direction: column;
   align-items: center;
   gap: 4px;
 }
 
 .evo-node.is-clickable {
   cursor: pointer;
   border-radius: 10px;
   padding: 4px;
   transition: background 0.15s ease;
 }
 
 .evo-node.is-clickable:hover,
 .evo-node.is-clickable:focus-visible {
   background: rgba(47, 107, 68, 0.25);
   outline: 1px solid #4fae78;
 }
 
 .evo-node img {
   width: 96px;
   display: block;
@@ -748,86 +792,81 @@ body {
   image-rendering: pixelated;
 }
 
 .evo-requirement-text {
   font-size: 10px;
   color: #7bdc65;
 }
 
 .evo-chevron {
   width: 30px;
   height: 20px;
   border-left: 3px solid #7bdc65;
   border-bottom: 3px solid #7bdc65;
   transform: rotate(-45deg);
 }
 
 .mega-split-row,
 .evo-branch-row {
   display: flex;
   gap: 14px;
   justify-content: center;
   width: 100%;
 }
 
 .eevee-evolution-layout {
-  position: relative;
-  width: min(100%, 300px);
-  height: 300px;
-  margin: 8px auto;
+  width: 100%;
+  display: grid;
+  grid-template-columns: 1fr;
+  gap: 12px;
+  justify-items: center;
 }
 
-.eevee-center {
-  position: absolute;
-  top: 50%;
-  left: 50%;
-  transform: translate(-50%, -50%);
-  z-index: 2;
+.eevee-base {
+  width: 100%;
+  display: flex;
+  justify-content: center;
 }
 
-.eevee-ring {
-  position: absolute;
-  inset: 0;
+.eevee-branches {
+  width: 100%;
+  display: grid;
+  grid-template-columns: repeat(2, minmax(122px, 1fr));
+  gap: 12px 8px;
 }
 
 .eevee-orbit-item {
-  --radius: 122px;
-  position: absolute;
-  top: 50%;
-  left: 50%;
-  transform: rotate(calc(360deg / var(--total) * var(--index)))
-    translateY(calc(var(--radius) * -1));
-  transform-origin: center;
   display: flex;
   flex-direction: column;
   align-items: center;
   gap: 4px;
-  width: 122px;
+  width: 100%;
+  min-width: 0;
 }
 
 .eevee-orbit-item .evo-transition {
-  transform: scale(0.72);
+  transform: none;
 }
 
 .mega-path,
 .evo-branch-column,
 .mega-branch {
   display: flex;
   flex-direction: column;
   align-items: center;
   gap: 6px;
 }
 
 .pokemon-view.is-mystic .info-panel,
 .pokemon-view.is-mystic .art-frame,
 .pokemon-view.is-mystic .search-preview .preview-card,
 .pokemon-view.is-mystic .mobile-art-nav {
   border-color: #d9b44a;
   box-shadow: 0 0 12px rgba(217, 180, 74, 0.25);
 }
 
 .pokemon-view.is-mystic .info-panel {
   background: linear-gradient(180deg, #221931, #0b1611);
 }
 
 .pokemon-view.is-mystic .block h3,
 .pokemon-view.is-mystic .evolution-title,
@@ -1518,52 +1557,37 @@ body {
     height: 20px;
   }
 
   .mobile-art-nav {
     display: flex;
   }
 
   .mobile-art-nav-dex {
     font-size: clamp(9px, 2.3vw, 11px);
   }
 
   .nav-zone,
   .nav-btn {
     display: none;
   }
 
   .evolution-panel::before {
     display: none;
   }
 
   .evolution-chain.vertical {
     padding-left: 0;
   }
 
   .eevee-evolution-layout {
-    height: auto;
-    width: 100%;
-    display: flex;
-    flex-direction: column;
-    align-items: center;
     gap: 10px;
   }
 
-  .eevee-center,
-  .eevee-ring,
-  .eevee-orbit-item {
-    position: static;
-    transform: none;
-    width: auto;
-  }
-
-  .eevee-ring {
-    display: flex;
-    flex-wrap: wrap;
-    justify-content: center;
-    gap: 8px;
+  .eevee-branches {
+    grid-template-columns: 1fr;
+    gap: 10px;
   }
 
   .eevee-orbit-item .evo-transition {
     transform: none;
   }
 }
diff --git a/css/pokemon.css b/css/pokemon.css
index 4c92e7e6531fbd9c85dabcb77825565413f25fda..15f64c3fa597287e516f94f26322ef76ac6fe62e 100644
--- a/css/pokemon.css
+++ b/css/pokemon.css
@@ -42,56 +42,82 @@ header h2 {
 
 header {
   display: flex;
   align-items: center;
   justify-content: center;
   position: relative;
   padding: 18px 24px;
   background: linear-gradient(180deg, #1f4d2b, #102018);
   border-bottom: 4px solid #7bdc65;
 }
 
 .title-container {
   text-align: center;
   line-height: 1.1;
 }
 
 header h1,
 header h2 {
   margin: 0;
 }
 
 
 .site-home-link {
   color: inherit;
   text-decoration: none;
+  display: inline-block;
+  outline: none;
+  -webkit-tap-highlight-color: transparent;
 }
 
 .site-home-link:hover {
   text-decoration: underline;
 }
 
+.site-home-link:focus,
+.site-home-link:focus-visible {
+  outline: none;
+  box-shadow: none;
+}
+
+.site-home-link.expanding {
+  animation: titleExpand 0.36s ease forwards;
+}
+
+@keyframes titleExpand {
+  0% {
+    transform: scale(1);
+    letter-spacing: normal;
+    text-shadow: 0 0 6px rgba(123, 220, 101, 0.4);
+  }
+  100% {
+    transform: scale(1.08);
+    letter-spacing: 1.5px;
+    text-shadow: 0 0 16px rgba(123, 220, 101, 0.9);
+  }
+}
+
 .logo {
   position: absolute;
   left: 20px;
   width: 90px;
   filter: drop-shadow(0 0 6px #7bdc65);
 }
 
 .title-container {
   text-align: center;
 }
 
 
 .controls {
   display: flex;
   gap: 10px;
   justify-content: center;
   padding: 15px;
   flex-wrap: wrap;
 }
 .controls input {
   background: #0b1611;
   color: #fff;
   border: 2px solid #2f6b44;
   padding: 10px 14px;
   border-radius: 10px;
diff --git a/js/moves.js b/js/moves.js
index 9567ceac795952aec6ddda9932f1dfd5c00441d3..801acadacdd16a57694a28e4d29106dea1edf221 100644
--- a/js/moves.js
+++ b/js/moves.js
@@ -143,25 +143,45 @@ function toggleMove(card) {
       const pp = ppToRPG(move.pp);
 
       details.innerHTML = `
         <div class="move-stats">
           <span>⚔ ${dmg}</span>
           <span>🎯 ${acc}</span>
           <span>⏳ ${pp}</span>
         </div>
 
         <p class="desc">${move.description_pt}</p>
 
         <button onclick="event.stopPropagation(); copyMove(\`${formatText(move,dmg,acc,pp)}\`)">
           Copiar golpe
         </button>
       `;
 
       details.dataset.loaded = "true";
     }
   }
 
   card.classList.toggle("open");
 }
 
 
 
+
+function setupSiteTitleAnimation() {
+  const homeLink = document.querySelector(".site-home-link");
+  if (!homeLink) return;
+
+  homeLink.addEventListener("click", (event) => {
+    if (event.metaKey || event.ctrlKey || event.shiftKey || event.altKey) return;
+
+    event.preventDefault();
+    homeLink.classList.remove("expanding");
+    void homeLink.offsetWidth;
+    homeLink.classList.add("expanding");
+
+    setTimeout(() => {
+      window.location.href = homeLink.getAttribute("href") || "index.html";
+    }, 360);
+  });
+}
+
+setupSiteTitleAnimation();
diff --git a/js/pokemon-view.js b/js/pokemon-view.js
index 347e10496556d4e979fbd5b8fc86ed3f3a02bc52..79d8b2de4e3891fbe5cbf8213231ebfa69c23e32 100644
--- a/js/pokemon-view.js
+++ b/js/pokemon-view.js
@@ -1479,109 +1479,107 @@ async function renderEvolutionChain(speciesUrl) {
     const speciesData = await getSpeciesData(node.species.url);
 
     return {
       name: node.species.name,
       speciesName: speciesData.name,
       details: node.evolution_details?.[0] || null,
       evolvesTo: await Promise.all(node.evolves_to.map(traverse)),
       megaForms: extractMegaForms(speciesData),
     };
   }
 
   const chain = await traverse(chainData.chain);
   await renderChainNode(chain, container);
 }
 
 document.querySelectorAll(".nav-zone").forEach((zone) => {
   zone.addEventListener("click", () => {
     const dir = zone.dataset.nav;
     if (dir === "prev") goPrev();
     if (dir === "next") goNext();
   });
 });
 
 async function renderChainNode(node, container) {
   if (node.name === "eevee" && node.evolvesTo.length >= 6) {
-    await renderEeveeCircle(node, container);
+    await renderEeveeLayout(node, container);
     return;
   }
 
   const wrapper = await createEvolutionNode(node.name);
   container.appendChild(wrapper);
 
   if (node.evolvesTo.length > 1) {
     const branchRow = document.createElement("div");
     branchRow.className = "evo-branch-row";
 
     for (const next of node.evolvesTo) {
       const branchColumn = document.createElement("div");
       branchColumn.className = "evo-branch-column";
 
       branchColumn.appendChild(await createEvolutionTransition(next.details));
       await renderChainNode(next, branchColumn);
       branchRow.appendChild(branchColumn);
     }
 
     container.appendChild(branchRow);
   } else {
     for (const next of node.evolvesTo) {
       container.appendChild(await createEvolutionTransition(next.details));
       await renderChainNode(next, container);
     }
   }
 
   if (node.megaForms.length > 0) {
     container.appendChild(await createMegaBranch(node.megaForms));
   }
 }
 
-async function renderEeveeCircle(node, container) {
+async function renderEeveeLayout(node, container) {
   const eeveeLayout = document.createElement("div");
   eeveeLayout.className = "eevee-evolution-layout";
 
-  const center = document.createElement("div");
-  center.className = "eevee-center";
-  center.appendChild(await createEvolutionNode(node.name));
-  eeveeLayout.appendChild(center);
+  const eeveeBase = document.createElement("div");
+  eeveeBase.className = "eevee-base";
+  eeveeBase.appendChild(await createEvolutionNode(node.name));
+  eeveeLayout.appendChild(eeveeBase);
 
-  const ring = document.createElement("div");
-  ring.className = "eevee-ring";
+  const eeveeBranches = document.createElement("div");
+  eeveeBranches.className = "eevee-branches";
 
-  for (const [index, next] of node.evolvesTo.entries()) {
+  for (const next of node.evolvesTo) {
     const orb = document.createElement("div");
     orb.className = "eevee-orbit-item";
-    orb.style.setProperty("--index", index);
-    orb.style.setProperty("--total", node.evolvesTo.length);
 
     const transition = await createEvolutionTransition(next.details);
     const evolvedNode = await createEvolutionNode(next.name);
     orb.appendChild(transition);
     orb.appendChild(evolvedNode);
-    ring.appendChild(orb);
+    eeveeBranches.appendChild(orb);
   }
 
-  eeveeLayout.appendChild(ring);
+  eeveeLayout.appendChild(eeveeBranches);
   container.appendChild(eeveeLayout);
 }
 
 function applySpecialTheme(species) {
   const pokemonView = document.querySelector(".pokemon-view");
   if (!pokemonView) return;
 
   const isSpecial = species.is_legendary || species.is_mythical;
   pokemonView.classList.toggle("is-mystic", isSpecial);
 }
 
 async function getPokemonSprite(name) {
   if (pokemonSpriteCache.has(name)) return pokemonSpriteCache.get(name);
 
   try {
     const poke = await fetch(`https://pokeapi.co/api/v2/pokemon/${name}`).then((r) =>
       r.json(),
     );
 
     const sprite =
       poke.sprites.other["official-artwork"].front_default ||
       poke.sprites.front_default ||
       "assets/unknown.png";
 
     pokemonSpriteCache.set(name, sprite);
@@ -1717,63 +1715,79 @@ function getEvolutionRequirement(details, override = {}) {
       text: formatPokemonName(details.item.name),
     };
   }
 
   if (details.min_level) {
     return {
       itemName: "rare_candy",
       text: `Nv. ${details.min_level}`,
     };
   }
 
   if (details.trigger?.name === "trade") {
     return {
       itemName: "link_cable",
       text: "Troca",
     };
   }
 
   return {
     itemName: "rare_candy",
     text: "Evolui",
   };
 }
 
 async function getEvolutionItemImage(itemName) {
-  const normalized = itemName.toLowerCase().replace(/\s+/g, "_");
+  const normalized = itemName
+    .toLowerCase()
+    .normalize("NFD")
+    .replace(/[\u0300-\u036f]/g, "")
+    .replace(/['’.]/g, "")
+    .replace(/\s+/g, "_")
+    .replace(/-/g, "_");
+
   if (itemSpriteCache.has(normalized)) return itemSpriteCache.get(normalized);
 
-  const localPath = `assets/evolution items/${normalized}.png`;
+  const localCandidates = [
+    `assets/evolution items/${normalized}.png`,
+    `assets/evolution items/${normalized.replace(/_/g, " ")}.png`,
+    `assets/evolution items/${normalized.replace(/_/g, "-")}.png`,
+    `assets/evolution items/${normalized
+      .replace(/_/g, " ")
+      .replace(/\b\w/g, (char) => char.toUpperCase())}.png`,
+  ];
 
-  try {
-    const localResponse = await fetch(encodeURI(localPath), { method: "HEAD" });
-    if (localResponse.ok) {
-      itemSpriteCache.set(normalized, localPath);
-      return localPath;
+  for (const candidatePath of localCandidates) {
+    try {
+      const localResponse = await fetch(encodeURI(candidatePath), { method: "HEAD" });
+      if (localResponse.ok) {
+        itemSpriteCache.set(normalized, candidatePath);
+        return candidatePath;
+      }
+    } catch {
+      // tenta próximo candidato
     }
-  } catch {
-    // fallback para pokeapi
   }
 
   try {
     const itemData = await fetch(`https://pokeapi.co/api/v2/item/${normalized}`).then((r) =>
       r.json(),
     );
     const sprite = itemData.sprites.default || "assets/forms/mega.png";
     itemSpriteCache.set(normalized, sprite);
     return sprite;
   } catch {
     return "assets/forms/mega.png";
   }
 }
 
 async function createMegaBranch(megaForms) {
   const megaContainer = document.createElement("div");
   megaContainer.className = "mega-branch";
 
   if (megaForms.length === 1) {
     const mega = megaForms[0];
     megaContainer.appendChild(
       await createEvolutionTransition(null, {
         itemName: mega.stoneName,
         text: mega.label,
       }),
@@ -2006,47 +2020,67 @@ ${text}
 `;
 
   navigator.clipboard.writeText(formatted).then(() => showCopyToast());
 });
 
 /* ===== BOTÃO DE CHORO ===== */
 document.querySelector(".cry-btn")?.addEventListener("click", playCry);
 
 /* ===== NAVEGAÇÃO ===== */
 document.querySelector(".nav-btn.left")?.addEventListener("click", goPrev);
 
 document.querySelector(".nav-btn.right")?.addEventListener("click", goNext);
 
 document.getElementById("toggleShinyBtn")?.addEventListener("click", () => {
   isShinyActive = !isShinyActive;
   updateArtworkImage();
 });
 
 document.querySelectorAll("[data-mobile-nav]").forEach((btn) => {
   btn.addEventListener("click", () => {
     if (btn.dataset.mobileNav === "prev") goPrev();
     if (btn.dataset.mobileNav === "next") goNext();
   });
 });
 
+function setupSiteTitleAnimation() {
+  const homeLink = document.querySelector(".site-home-link");
+  if (!homeLink) return;
+
+  homeLink.addEventListener("click", (event) => {
+    if (event.metaKey || event.ctrlKey || event.shiftKey || event.altKey) return;
+
+    event.preventDefault();
+    homeLink.classList.remove("expanding");
+    void homeLink.offsetWidth;
+    homeLink.classList.add("expanding");
+
+    setTimeout(() => {
+      window.location.href = homeLink.getAttribute("href") || "index.html";
+    }, 360);
+  });
+}
+
 document.addEventListener("DOMContentLoaded", () => {
+  setupSiteTitleAnimation();
+
   [nameInput, dexInput].forEach((input) =>
     input.addEventListener("input", updateSearchPreview),
   );
 
   document.querySelectorAll(".move-tab-buttons .tab-btn").forEach((btn) => {
     btn.addEventListener("click", () => {
       const tab = btn.dataset.movetab;
 
       document
         .querySelectorAll("#level-tab, #other-tab")
         .forEach((t) => t.classList.remove("active"));
 
       document
         .querySelectorAll(".move-tab-buttons .tab-btn")
         .forEach((b) => b.classList.remove("active"));
 
       document.getElementById(`${tab}-tab`)?.classList.add("active");
       btn.classList.add("active");
     });
   });
 });
diff --git a/js/pokemon.js b/js/pokemon.js
index 2a8000dc69f6172773f39b619038e345ad9c547e..47fafe6c50de8d943b191d65c15ae31ef54d32f4 100644
--- a/js/pokemon.js
+++ b/js/pokemon.js
@@ -280,51 +280,70 @@ function filterByType(select) {
 
 function goToPokemon(dex) {
   const id = Number(dex);
 
   if (!id || id < 1 || id > 1025) return;
 
   window.location.href = `pokemon-view.html?id=${id}`;
 }
 
 function capitalize(text) {
   return text.charAt(0).toUpperCase() + text.slice(1);
 }
 
 function getGeneration(dex) {
   if (dex <= 151) return 1;
   if (dex <= 251) return 2;
   if (dex <= 386) return 3;
   if (dex <= 493) return 4;
   if (dex <= 649) return 5;
   if (dex <= 721) return 6;
   if (dex <= 809) return 7;
   if (dex <= 905) return 8;
   return 9;
 }
 
+function setupSiteTitleAnimation() {
+  const homeLink = document.querySelector(".site-home-link");
+  if (!homeLink) return;
+
+  homeLink.addEventListener("click", (event) => {
+    if (event.metaKey || event.ctrlKey || event.shiftKey || event.altKey) return;
+
+    event.preventDefault();
+    homeLink.classList.remove("expanding");
+    void homeLink.offsetWidth;
+    homeLink.classList.add("expanding");
+
+    setTimeout(() => {
+      window.location.href = homeLink.getAttribute("href") || "index.html";
+    }, 360);
+  });
+}
+
 document.addEventListener("DOMContentLoaded", () => {
+  setupSiteTitleAnimation();
   loadHabitats();
   loadPokemons();
 
   // Pesquisa por nome
   document.getElementById("searchInput").addEventListener("input", (e) => {
     searchPokemon(e.target.value);
   });
 
   // Pesquisa por número
   document.getElementById("dexInput").addEventListener("keydown", (e) => {
     if (e.key === "Enter") {
       goToPokemon(e.target.value);
     }
   });
 
   // Botões
   document
     .getElementById("toggleViewBtn")
     .addEventListener("click", toggleView);
 
   document
     .getElementById("openFiltersBtn")
     .addEventListener("click", openFilters);
 
   document
